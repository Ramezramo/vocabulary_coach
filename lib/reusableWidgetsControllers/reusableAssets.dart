import 'dart:ui';

///Note that if you want to run the fire base x operations connect the code with the firebase by going to the firebase website
///and then go to the databasexoperations and make it usable then run the cloud store in the pubspec.yaml

/// reusable widget constants variables just change it from here
const cPrimaryColor = Color(0xff100B20);
const cTranstionDuration = Duration(milliseconds: 250);


/// splash page [the app launching screen]

const splashText = "here apps splash text";
const cLogo = 'assets/images/Logo.png';

const cTestImage = 'assets/images/test_image.png';