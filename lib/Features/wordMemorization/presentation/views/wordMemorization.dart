
import 'package:flutter/material.dart';


class wordMemorizationPage extends StatelessWidget {
  const wordMemorizationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "word Memorization Page"
        ),
      ),
    );
  }
}
